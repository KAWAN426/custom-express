{
  "name": "custom-express",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": "16.x"
  },
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "setYarnBerry":"npm install -g yarn && yarn set version berry && yarn install && yarn add @yarnpkg/sdks -D && yarn dlx @yarnpkg/sdks vscode"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cluster": "latest",
    "compression": "latest",
    "cors": "latest",
    "dotenv": "latest",
    "express": "latest",
    "express-async-handler": "latest",
    "helmet": "latest",
    "os": "latest"
  },
  "devDependencies": {
    "nodemon": "latest"
  }
}
